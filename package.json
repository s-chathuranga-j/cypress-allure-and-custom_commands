{
  "name": "cypress-allure-reporting",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "run-allure": "npx cypress run --browser chrome --headless --spec 'cypress/e2e/1-getting-started/**.js' --reporter mocha-allure-reporter"
  },
  "author": "Chathuranga",
  "license": "ISC",
  "devDependencies": {
    "@shelex/cypress-allure-plugin": "^2.40.0",
    "allure-commandline": "^2.22.4",
    "cypress": "^12.15.0",
    "mocha-allure-reporter": "^1.4.0"
  }
}
